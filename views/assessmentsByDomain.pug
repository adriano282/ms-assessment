extends layouts/main-layout.pug

block styles
    link(rel="stylesheet", href="/css/custom.css")


block content
    main
        
        h3.tile-title Notas Microservices do Domínio #{domain}
            div
                canvas(id="lineChartDemo", width="300" height="120")
        
        script(type='text/javascript', src="https://cdn.jsdelivr.net/npm/chart.js")
        script(type='text/javascript').
            
            var ctxl = document.getElementById('lineChartDemo');
            var lineChart = new Chart(ctxl, !{JSON.stringify(setup)} );